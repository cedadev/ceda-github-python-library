apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name }}
  description: ${{ values.description }}
  annotations:
    github.com/project-slug: cedadev/${{ values.repositoryName }}
spec:
  type: library
  owner: ${{ values.author }}
  lifecycle: experimental
